<form method="post" action="{{ path('app_bus_delete', {'imatriculation': bu.imatriculation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bu.imatriculation) }}">
    <button class="btn">Delete</button>
</form>
